# Naming — Zombie Survival Score / WROCŁAW

Locked GOAL 006 / 034. Player-facing. Do not invent a third stack.

## Stack (everywhere a stranger looks)

| Layer | Exact |
| --- | --- |
| Product title | `Zombie Survival Score` |
| City tag | `WROCŁAW` (ALL CAPS, Polish Ł) |
| Chrome | `PLONKU / SCORE` |
| Score label | `WYNIK` — **never** the city name as the number |
| Pairing | Title and city together as **Zombie Survival Score / WROCŁAW** |

Forbidden as the live score line: `Wrocław 438`, `Wrocław  438`.

## Surfaces

| Surface | What it shows |
| --- | --- |
| Window / `runner.json` `title` | Zombie Survival Score |
| Start | chrome + title + tag `WROCŁAW` |
| Play HUD | box `WYNIK` + tag `WROCŁAW` |
| Game over | `KONIEC GRY` + `TWÓJ WYNIK` + city still `WROCŁAW` |
| Handoff `displayTitle` | `Zombie Survival Score / WROCŁAW` |
| Choose-city row (036) | playable row labeled `WROCŁAW` |

Sentence case `Wrocław` is OK in body copy (`Przetrwaj Wrocław…`), not as the score prefix.
