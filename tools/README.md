# tools

Local helper scripts and downloaded editor/CLI bits for this lab.

- Commit shared scripts here when a later task needs them.
- Keep machine-specific binaries and caches in `tools/local/` (gitignored).
